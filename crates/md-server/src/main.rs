//! md-server — the md-mcp MCP server binary.
//!
//! Exposes the [`md_core`] vault model as MCP tools over a stdio transport. The
//! rmcp bootstrap, configuration, and tool wiring land in their own change; this
//! is the scaffold entry point.

fn main() {
    eprintln!("md-server: not yet wired (scaffold)");
}
